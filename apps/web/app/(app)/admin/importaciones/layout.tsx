import type { ReactNode } from 'react';

export const metadata = { title: 'Administración: importaciones' };

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
