import type { ReactNode } from 'react';

export const metadata = { title: 'Administración: tipos de permiso' };

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
