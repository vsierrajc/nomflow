import type { ReactNode } from 'react';

export const metadata = { title: 'Administración: cuentas y roles' };

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
