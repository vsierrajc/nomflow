import type { ReactNode } from 'react';

export const metadata = { title: 'Administración: áreas, cargos y centros de costo' };

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
