import type { ReactNode } from 'react';

export const metadata = { title: 'Administración: empresas y logo' };

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
