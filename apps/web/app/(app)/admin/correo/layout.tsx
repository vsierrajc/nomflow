import type { ReactNode } from 'react';

export const metadata = { title: 'Administración: correo saliente' };

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
