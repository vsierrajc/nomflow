import type { ReactNode } from 'react';

export const metadata = { title: 'Administración: nómina publicada' };

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
