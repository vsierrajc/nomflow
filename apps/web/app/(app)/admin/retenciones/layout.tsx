import type { ReactNode } from 'react';

export const metadata = { title: 'Administración: certificados de retención' };

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
