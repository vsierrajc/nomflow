import type { ReactNode } from 'react';

export const metadata = { title: 'Administración: conceptos de nómina' };

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
