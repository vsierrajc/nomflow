import type { ReactNode } from 'react';

export const metadata = { title: 'Administración: Períodos de vacaciones' };

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
