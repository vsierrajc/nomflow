import type { ReactNode } from 'react';

export const metadata = { title: 'Mis certificados de retención' };

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
