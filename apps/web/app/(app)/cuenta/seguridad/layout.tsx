import type { ReactNode } from 'react';

export const metadata = { title: 'Mi cuenta: verificación en dos pasos' };

export default function Layout({ children }: { children: ReactNode }) {
  return children;
}
