# Changelog

## Unreleased
- Documentación inicial: SSD canónica, STATUS, ADR-001 (stack) y matriz de trazabilidad.
- Monorepo npm workspaces: apps/api (NestJS 11, /health), apps/web (Next 16), CI (format, lint, tipos, build, tests, audit, gitleaks, CodeQL), docker-compose de desarrollo.
- Esquema inicial (Drizzle): cuentas, asignaciones de rol y auditoría; hash Argon2id de claves; pruebas de integración con PostgreSQL.
- Alta administrativa de cuentas (servicio): exige HR_ADMIN vigente, EST=V con contrato único, correo normalizado, clave temporal Argon2id y auditoría. Tabla employee_snapshots.
- Login y sesiones: POST /auth/login, GET /auth/me, POST /auth/logout; cookie HttpOnly SameSite=Strict, token de sesión hasheado, CSRF (HMAC) en métodos no seguros, bloqueo tras 5 intentos (15 min), expiración por inactividad (30 min) y absoluta (8 h), revocación por cuenta. Requiere SESSION_SECRET.
- Verificación por SMTP y activación: código aleatorio de un solo uso (8 caracteres, HMAC, 15 min, 5 intentos, máx. 3 envíos/hora) enviado al correo de EMPLEADOS; POST /auth/activate (clave temporal + código + clave nueva ≥12) y POST /auth/verify-email/resend; nodemailer con TLS exigido por defecto; Mailpit en docker-compose.
- Endpoint POST /admin/accounts (sesión + rol HR_ADMIN vigente + reautenticación en los últimos 10 min); POST /auth/reauth; guards Roles/RecentAuth; los administradores (HR_ADMIN/SYSTEM_ADMIN) pueden iniciar sesión sin contrato EST=V; comando `npm run admin:bootstrap` para crear el primer administrador.
- Importación de EMPLEADOS (.xlsx): POST /admin/imports/employees (staging, validación de encabezados, tipos, EST V/C, unicidad, un contrato vigente), GET /admin/imports/:id y /errors, POST /admin/imports/:id/apply (transacción con bloqueo, conteo verificado, conciliación por N_IDE+N_CONT, EST=C revoca sesiones). employee_snapshots con las 24 columnas de EMPLEADOS.
- Empresas y catálogos organizacionales: CRUD de empresas con control de versión (GET/POST/PUT /admin/companies); importación Excel de AREA, CCOSTO, CARGO y TIPO_CONTRATO (POST /admin/imports/catalogs/:kind, misma vista previa/aplicación atómica), historial de cambios de nombre, listado GET /admin/imports/catalogs/:kind; EMPLEADOS se valida contra empresa y catálogos publicados (código inexistente = error, descripción distinta = advertencia).
- Roles con alcance y jefes de área: POST/GET /admin/accounts/:id/roles (nadie se concede roles a sí mismo; HR_ADMIN/SYSTEM_ADMIN solo los concede SYSTEM_ADMIN; AREA_MANAGER exige empresa y área existentes); POST /admin/areas/managers, PUT /admin/areas/managers/:id/end y GET /admin/areas/:cEmp/:cArea/managers con vigencia sin solapes y bloqueo por área; resolveAreaManager(cEmp, cArea, fecha).
- Graphify: mapa del código local (`npm run graph`, `graphify affected/explain/path`), `.graphifyignore` que excluye Excel y datos, hooks locales, job informativo `graph` en el CI que publica el informe como artefacto, `CLAUDE.md` y `docs/graphify.md`.
- POST /auth/change-password: cada usuario cambia su clave con sesión, CSRF y clave actual (mínimo 12 caracteres, distinta de la actual); los intentos fallidos cuentan para el bloqueo; cierra las demás sesiones y conserva la actual; se audita sin guardar claves.
- Web: pantallas de ingreso, activación de cuenta (con reenvío de código), cambio de clave e inicio con perfil y roles; la web habla con la API solo a través del proxy `/api` (sin CORS); CSP en producción; GET /auth/me devuelve correo, nombre y roles vigentes.
- Stack local: `npm run stack:up|status|down` (scripts/stack.sh) con PostgreSQL, Redis, MinIO (puertos 9100/9101), Mailpit, API y web; `.env` local generado con SESSION_SECRET aleatorio; docs/local-stack.md.
- Pruebas e2e de la interfaz con Playwright/Chromium (16): ingreso, sesión, cambio de clave, activación con el código leído de Mailpit, cabeceras/CSP, accesibilidad (axe WCAG 2 A/AA), móvil, modo oscuro y teclado; job informativo `e2e` en el CI; `NEXT_DIST_DIR` para compilar la web de pruebas aparte.
- Volantes de pago (ESS-PAY-001): importación de NOMINA por alcance PER + N_LIQ (POST /admin/imports/payroll) con validación, conciliación de conteo y sumas exactas de DEV/DED, versiones (una sola publicada, correcciones = nueva versión, mismo contenido rechazado); GET /me/payroll y GET /me/payroll/:per/:nLiq/:contrato/pdf?mode=SIN_AJUSTE|ENTERO_SUPERIOR con PDF (pdfkit), acceso solo a los propios, auditoría de descargas sin importes; modo por defecto configurable por empresa; página web /volantes.
- Administración (ESS-ADM-001, API): catálogo de conceptos de nómina con unidades (CRUD + importación de CONCEPTOS.xlsx) y cantidad con unidad en el volante; logo de empresa versionado y reemplazable (PNG/JPEG hasta 512 KB, 64-2000 px), con logo genérico incluido; CRUD de áreas, centros de costo, cargos y tipos de contrato (baja lógica bloqueada si están en uso, historial de nombres); CRUD de empleados con motivo obligatorio, control de versión e historial de cambios; gestión de cuentas (listar, bloquear, desbloquear, restablecer clave); terminar roles; resumen, historial de importaciones y acceso a volantes ajenos con motivo. Todas las rutas /admin exigen perfil administrador (HR_ADMIN o SYSTEM_ADMIN), verificado por una prueba que recorre cada ruta.
- Auditoría: cada petición HTTP de todos los usuarios queda registrada (usuario, método, plantilla de ruta, estado, duración, IP, agente, id de petición) sin cuerpos, claves, códigos, tokens ni identificadores reales; GET /admin/audit consulta peticiones y actividades con filtros por tipo, usuario, método, estado, ruta y fechas.
- Administración (ESS-ADM-002, web): área administrativa en /admin solo para perfil administrador (acceso restringido para el resto; el servidor sigue siendo quien autoriza): resumen, empresas con carga y versiones de logo, catálogos (áreas, centros de costo, cargos, tipos de contrato) con CRUD e importación, conceptos de nómina con unidades, empleados (consulta, alta y corrección excepcional con motivo, baja y reactivación), cuentas y roles (crear cuenta con clave temporal única, bloquear, desbloquear, restablecer, conceder y terminar roles, asignar jefe de área), importaciones con vista previa y reporte de errores, nómina publicada con descarga de volantes ajenos con motivo, y visor de auditoría de peticiones y actividades. La reautenticación se pide en un diálogo y la acción se reintenta.
- Correcciones halladas por las pruebas de navegador: la edición de empresas ignoraba el modo del volante; la baja de un empleado exigía validar catálogos y ficha completa; ids duplicados entre diálogos y filtros; campo opcional vacío rechazado en importaciones; tablas desplazables sin acceso por teclado.
- Rediseño de la interfaz (ESS-UX-001, guía DisenhoUIX.md): sistema visual con tokens semánticos y modo oscuro verificado (contraste WCAG AA), escala tipográfica y de espacio en rem, marco de aplicación con navegación por rol y menú plegable en móvil, enlace «Saltar al contenido», títulos por ruta (API de metadatos), acceso/activación/cambio de clave con validación junto a cada campo, ayuda y requisitos de clave en vivo, mostrar/ocultar clave, prevención de envíos duplicados, inicio como espacio de trabajo con tareas reales y roles en lenguaje comprensible, volantes con estados de carga/vacío/error, y el área administrativa sobre el mismo marco. Rutas agrupadas en (public) y (app) sin cambiar URL. Diseño y evidencia en docs/design/.
- Traspaso y Graphify: `docs/HANDOFF.md` para la próxima sesión; Graphify con soporte SQL (`graphifyy[sql]` fijado en 0.9.67, también en el CI) que indexa las migraciones (58 nodos), guía actualizada con identificadores para nombres ambiguos.
- ESS-TAX-001: certificados de retención por carga masiva desde carpeta (`TAX_CERT_INBOX_DIR`), versionados, con consulta y descarga del propio empleado y pantalla administrativa; migración 0015.
- CI: CodeQL sin Code scanning (repositorio privado): `upload: never`, SARIF como artefacto y el job falla si hay hallazgos; ya no es informativo.
- Repositorio público: Code scanning con CodeQL del workflow (la configuración predeterminada de GitHub queda desactivada para no chocar), escaneo de secretos con protección de push y alertas de Dependabot activados.
- CI: el job de CodeQL ignora las alertas descartadas como falso positivo en Security (2 descartadas: hash del token de sesión y cookie de sesión).
- Dependencias: `overrides` fijan `uuid` 11.1.1 (por exceljs) y `esbuild` 0.28.2, con lo que `npm audit` queda en 0 y se cierran las 2 alertas de Dependabot; lockfile regenerado.
- UI: la pantalla de ingreso (`/login`) pasa a una portada de portal: panel de marca con titular y servicios (volantes, certificados de retención, vacaciones y permisos como «próximamente») junto a la tarjeta de acceso; mismos tokens de color, modo oscuro y adaptación a teléfono. La activación de cuenta no cambia.
- ESS-LEAVE-001, parte de ESS-LEAVE-002 y ESS-HOL-001: `PROG_VAC` (CRUD y carga Excel con vista previa), festivos (borradores versionados y publicación), cálculo de días hábiles con retorno y cruce de año, vista previa para el empleado; migración 0016.
- ESS-LEAVE-002/003: solicitud de vacaciones (uno o varios períodos), revisiones, aprobación del jefe de área asignado y aprobación final con descuento transaccional de `DISP` y registro en `VACACIONES`; pantallas `/vacaciones` y `/aprobaciones`; migración 0017.
- ESS-HOL-001: configuración administrativa del servicio de festivos (URL y clave cifrada con AES-256-GCM, nunca devuelta al navegador) y consulta año por año desde el servidor que deja un borrador con origen API y su diferencia frente al publicado; errores 401, 429, 5xx, datos inválidos y redirecciones conservan el último calendario publicado; migración 0018.
- Vacaciones: la solicitud solo acepta períodos de `PROG_VAC` del contrato vigente del propio empleado, activos y con días disponibles; un período liquidado se rechaza con un error explícito (`PERIOD_NOT_AVAILABLE`) en la vista previa, el envío y la propuesta del jefe.
- Vacaciones: el aprobador final es por empresa. El rol `VACATION_FINAL_APPROVER` exige una empresa registrada al asignarse; su bandeja, el detalle y la firma se limitan a las solicitudes de sus empresas (de otra empresa: 404; con el rol sin empresa: sin acceso). Sigue siendo un usuario común con un rol adicional, no un administrador.
- ESS-PERM-001: permisos. Catálogo de tipos configurable por el administrador (soporte obligatorio, horas, máximo de días), solicitud del empleado con soporte PDF/PNG/JPEG validado por contenido, y decisión **solo del jefe de área** asignado a la persona (aprobar o rechazar con motivo); sin aprobación final. No consumen `PROG_VAC` y no se cruzan con otros permisos ni con vacaciones aprobadas. Pantallas `/permisos`, `/aprobaciones` (pestaña Permisos) y `/admin/tipos-permiso`; migración 0019.
- PROG_VAC, alta manual: el empleado se elige de una lista de empleados activos (con búsqueda por nombre o identificación) y N_CONT se completa solo con el contrato vigente (solo lectura); el servidor también exige un empleado activo y su contrato vigente (`GET /admin/prog-vac/employees`). La carga por Excel no cambia.
- API de festivos: si la URL responde una página y no JSON (por ejemplo la portada del sitio en lugar de `/api/v1/festivos`), el mensaje lo dice y sugiere la dirección del API; antes se informaba como «datos incompletos».
- Festivos: botón «Ver festivos» por calendario (fecha con día de la semana y nombre) y, en un borrador, comparación con el calendario publicado del mismo año (nuevos, que ya no vienen y con otro nombre), para revisarlo antes de publicar.
- Pruebas: estabilizada la prueba de cambio de clave (esperaba mal el cierre de sesión y fallaba de forma intermitente en CI).
- Pruebas: las de festivos y API de festivos ya no dependen del estado previo de la base (eligen un año libre y parten de una configuración vacía); se comprobó con repeticiones.
- Festivos, regla de consulta: la tabla local manda. Si al calcular (vista previa, envío o propuesta del jefe) un año no tiene calendario publicado y hay servicio configurado, NOMFLOW lo consulta solo, lo carga y lo publica, atribuido a quien configuró el servicio y con auditoría `HOLIDAY_API_AUTOLOAD`; un año con información nunca vuelve a consultarse. Una sola consulta aunque lleguen varias peticiones a la vez, y tras un fallo no se insiste durante 10 minutos. La aprobación final no consulta (no llama a servicios dentro de su transacción). La consulta manual del administrador sigue dejando un borrador para revisar.
- PROG_VAC, alta manual: se quita la caja de búsqueda; el empleado se elige solo de la lista de valores, cuya etiqueta pasa a «Identificación», y la API de la lista trae hasta 1000 empleados activos (antes 200).
- Correo saliente configurable desde la administración (`/admin/correo`, migración 0020): servidor, puerto, SMTPS, exigir STARTTLS, usuario y clave (cifrada, nunca devuelta), correo de origen (`fromEmail`, p. ej. nomflow@gr4l.co) y nombre del remitente, con envío de prueba y clasificación de errores (conexión, TLS, autenticación, rechazo). Los correos del sistema usan esa configuración al enviar (caché de 30 s que se invalida al guardar) y, si no hay ninguna guardada, las variables `SMTP_*`. Un servidor sin TLS como el Postfix interno responde 502 a STARTTLS y se explica con un mensaje que indica desmarcar «Exigir STARTTLS».
- Cuentas de acceso: (1) el administrador puede **asignar una clave** a un empleado desde su ficha en `/admin/empleados` (sección «Cuenta de acceso»: el usuario es su correo; clave escrita o generada; no se devuelve ni se audita; cierra sus sesiones; una cuenta pendiente o con «exigir cambio» exige el código de activación por correo, y una activa puede conservar la clave asignada) y crear la cuenta con clave elegida; (2) **verificación en dos pasos opcional** por correo: el empleado la activa en `/cuenta/seguridad` con un código de 6 dígitos enviado a su correo, y desde entonces cada ingreso pide clave + código (vigencia 10 min, un solo uso, 5 intentos, máx. 5 códigos/hora de ingreso y 3 de activación; si el correo falla no se abre sesión); se desactiva con la clave, y el administrador ve el estado y puede desactivarla por recuperación. Migración 0021.
- Scripts `iniciar_app.sh` y `detener_app.sh` (envoltorios de `scripts/stack.sh`). `detener_app.sh` ahora ejecuta `docker compose down` (antes `stop`) y admite `--borrar-datos`, con confirmación escrita. PostgreSQL pasa a un volumen con nombre (`pgdata`): con el anónimo, `down` habría dejado la base huérfana.
